{
  "name": "shareroom",
  "version": "2.0.0",
  "scripts": {
    "prestart": "mkdir -p /var/data && mkdir -p /var/data/uploads && rm -rf uploads messages.db && ln -sf /var/data/uploads ./uploads && ln -sf /var/data/messages.db ./messages.db || true",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "sqlite3": "^5.1.6",
    "multer": "^1.4.5-lts.1"
  },
  "engines": {
    "node": ">=18.18.0 <19.0.0"
  }
}