services:
  - type: web
    name: shareroom-app
    env: node
    region: oregon
    plan: free
    runtime: node:18
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: shareroom-data
      mountPath: /var/data
    environment:
      - key: NODE_ENV
        value: production
    healthCheckPath: /